/**
 * Style Utilities
 * Common styling constants and utilities
 */

// Screen layout styles
export const screenStyles = {
  center: {
    display: 'flex', 
    justifyContent: 'center', 
    alignItems: 'center', 
    height: '100vh',
    fontFamily: 'Arial, sans-serif',
    flexDirection: 'column'
  }
}

// PowerBI embed styles
export const powerBIStyles = {
  container: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    width: '100%',
    height: '100%'
  }
}